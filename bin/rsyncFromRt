#!/bin/bash

## Grab specified folder or file from rtorrent.data to Deaddrop

DL="\"$1\""

SOURCEDIR=farligm@a.tetov.se:/home/farligm/v/rtorrent.data

TARGETDIR=/Volumes/JUDITH/Deaddrop/

rsync -vrze 'ssh -q' "$SOURCEDIR"/"$DL" --progress "$TARGETDIR"
