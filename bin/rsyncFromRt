#!/bin/bash

## Grab specified folder or file from rtorrent.data to Deaddrop

dl="\"$1\""

sourceDir=$server_user@$server_url:$server_media_folder

targetDir=$local_drop_dir

/usr/local/bin/rsync -vrze 'ssh -q' "$sourceDir"/"$dl" --progress "$targetDir"
